*.log
.DS_Store
.env
.env.*
!.env.example
!.env.production.example
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.composer
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build   <-- Commented out to allow deployment to cPanel without npm build
/public/hot
/public/storage
/storage/*.key
/storage/framework/cache/data/*
/storage/framework/sessions/*
/storage/framework/testing/*
/storage/framework/views/*
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Database backups
*.sql

# Stale deploy duplicate (not used at runtime; .cpanel.yml deploys repo root)
/.deploy/

# File deployment & temp
*.zip
*.tar.gz
*.rar
*.7z
*.tt
*.tmp

# AI assistant artifacts & one-off dev scripts
.aider*
composer.phar
/public/error_log
/image.png

# Artefak & staging deploy — besar, dan .deploy/ pernah berisi salinan
# database.sqlite. Tidak ada yang perlu masuk riwayat.
/deploy/
/.deploy/

# Credentials & signing keys — NEVER commit
ssh.md
informasi.md
*.keystore
ROTASI-RAHASIA.*

# Dump database — jangan pernah masuk riwayat
*.sql

# Font Material Symbols versi penuh (~450 KiB) yang diunduh skrip subset
# sebagai cache. Yang dipakai situs cuma hasil pangkasannya
# (material-symbols-subset.woff2), dan berkas ini bisa diunduh ulang kapan saja.
/resources/fonts/_ms-full.woff2

# Screenshot yang tertempel ke akar repo saat menempel gambar di chat.
# Sudah pernah tidak sengaja ikut ter-commit sekali (2 MB) -- dijaga di sini.
/image copy*.png
/Screenshot *.png
/aa.png
/aha.png

# Media yang diunggah lewat panel admin. Tempatnya di server, bukan di git.
/storage/app/public/packages
